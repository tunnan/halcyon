const path = require('path');

// Paths to different directories
const paths = {
    templateDir: 'pages' + path.sep + 'templates' + path.sep,
    inputDir: 'pages' + path.sep,
    outputDir: 'public' + path.sep,
}

module.exports = {
    paths,
}
